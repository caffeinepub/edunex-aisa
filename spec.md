# Specification

## Summary
**Goal:** Rename the brand from "EduNex Aisa" to "EduNex AI" everywhere in the codebase.

**Planned changes:**
- Replace all occurrences of "EduNex Aisa" with "EduNex AI" in `frontend/index.html` (page title)
- Replace all occurrences in the Navbar component (logo text, aria labels)
- Replace all occurrences in the Footer component (branding, copyright, mission statement)
- Replace all occurrences across remaining frontend files (Home.tsx, App.tsx, tailwind.config.js, index.css, etc.)
- Replace all occurrences in `backend/main.mo` (comments and string literals)

**User-visible outcome:** Every visible and hidden reference to "EduNex Aisa" is replaced with "EduNex AI" throughout the app, including the browser tab title, navbar, footer, and backend source.
